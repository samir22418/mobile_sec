# Migrations

The MVP creates tables with SQLAlchemy metadata for local speed.

This directory is reserved for Alembic migrations when the backend moves from
local MVP to production Postgres deployment.

