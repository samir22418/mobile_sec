<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ====================================================================
         AEGIS Agent — AndroidManifest.xml
         All permissions required for comprehensive device security telemetry
         ==================================================================== -->

    <!-- Network access — required for telemetry upload to AEGIS backend -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Package visibility — needed to enumerate installed apps (Android 11+) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- WorkManager foreground service — keeps the agent alive on strict devices -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Receive boot completed — restarts agent after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Needed to read Build.VERSION and SystemProperties for posture data -->
    <!-- Note: SystemProperties via reflection; no special permission needed but
         READ_PRIVILEGED_PHONE_STATE is required for some OEM properties — request
         only if device is fully managed (Device Owner mode via MDM) -->

    <application>

        <!-- BroadcastReceiver — listens for package install/remove events -->
        <receiver
            android:name=".data.apps.PackageEventReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver — restart agent after device reboot -->
        <receiver
            android:name=".data.system.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- WorkManager — declare the HiltWorkerFactory provider -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <!-- Disable auto-init of WorkManager so Hilt can own the factory -->
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
